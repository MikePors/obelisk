package com.example.demo;

import static com.example.demo.Formatter.normalizeInput;

public class FormatterClient {

    public String clean(String input) {
        return normalizeInput(input);
    }
}
